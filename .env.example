# Copy to .env and fill in values
# Luma session cookie — update when it expires (grab from browser devtools > Network > any luma.com request > Cookie header)
LUMA_COOKIE=luma.auth-session-key=...; luma.did=...; __cf_bm=...
